# 2026-06-15

## Bolt Invoice Processor Fix

Ugis asked whether the Bolt job worked. Cron showed the 09:00 run was scheduler-OK, but the useful work failed: "No new invoices; forwarding failed for 3 downloaded invoices."

Investigation:
- Latest unprocessed Bolt Work Profile report was Gmail message `19e912b1221ca244`, subject `Your Work Profile report for May 2026`.
- It contained `user_invoicing_report_202605.csv` with 3 invoice links.
- Reproduced the send path as a dry run. The encoded Gmail `raw` payload was about 158 KB, and passing it via `gws --json <payload>` raised `OSError: [Errno 7] Argument list too long`.
- The script had also lost the earlier fix that sourced `~/.bashrc` for `gws` keyring environment in cron.

Fix:
- Updated `scripts/bolt-invoices.sh` to source `~/.bashrc` when `GOG_KEYRING_*` variables are missing.
- Changed send path to write an RFC822 `.eml` file and call `gws gmail users messages send --json '{}' --upload <file> --upload-content-type message/rfc822`, avoiding command-line payload size limits.
- Stopped suppressing the Python send error stream, so future send failures are visible in cron output.

Verification:
- `bash -n scripts/bolt-invoices.sh` passed.
- `gws ... messages send --dry-run --upload ... --upload-content-type message/rfc822` passed.
- Ran `bash scripts/bolt-invoices.sh`; it downloaded 3 May invoice PDFs and successfully sent them to `ugis.springis@proofit.lv`.
- Added processed ID `19e912b1221ca244`; future runs should skip it and not resend.
- Re-ran `bash scripts/bolt-invoices.sh`; it skipped all already processed reports and did not resend.
- Triggered the actual OpenClaw cron job manually after the fix; it finished OK with summary `No new invoices`.

## JRT Ticket Sale Calendar Watch

Ugis asked for a daily OpenClaw job to watch Gmail for `@jrt.lv` / JRT ticket-sale announcements and create Google Calendar reminders when ticket sales start.

Done:
- Created OpenClaw isolated cron job `JRT Ticket Sale Check` (`1effb0e6-d219-4078-8f18-2a019c5eeb6a`) for 10:15 Europe/Riga daily.
- Job checks recent Gmail messages from JRT-related senders, treats email content as untrusted data, and only extracts ticket-sale start date/time.
- If a clear future sale-start timestamp is found, it creates a private 1-hour event in Ugis's primary Google Calendar with popup reminders 1 day, 1 hour, and 10 minutes before.
- Added duplicate-tracking state file `memory/jrt-ticket-sale-processed.json`.
- Gmail currently has only old JRT-related messages from 2009, 2010, and 2019; no current sale-start email.
- Manual test run finished OK: `JRT check OK: no new sale-start emails`.
