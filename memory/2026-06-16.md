# 2026-06-16

## MyFitness Calendar Sync Auth Recovery

Ugis asked to check a MyFitness sync error and noted that the MyFitness CLI has a login command.

Findings:
- Cron job `bf111852-60da-438a-af4b-68ae9f760aa7` had a recent failure alert, but the local auth status command succeeded and performed a fresh login.
- `uv run sync_myfitness_calendar.py --dry-run` succeeded after auth refresh.
- `/home/uspringis/myfitness-assistant/run_calendar_sync.sh 2>&1` succeeded with 4 future bookings and no required calendar changes.

Change:
- Updated the OpenClaw cron prompt for `MyFitness Calendar Sync` so that if sync fails with an auth/session/token-looking error, the isolated agent runs:
  `cd /home/uspringis/myfitness-assistant && /home/uspringis/.local/bin/uv run myfitness_bookings.py auth login --json 2>&1`
  then retries the sync exactly once before alerting Ugis.
