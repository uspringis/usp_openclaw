# 2026-07-19

## MyFitness Calendar Sync Timeout Hardening

- Diagnosed recurring `httpx.ReadTimeout` failures against the MyFitness group-training page. MyFitness intermittently took longer than the client's 30-second read timeout; authentication and Google Calendar were healthy.
- Updated `/home/uspringis/myfitness-assistant/myfitness_bookings.py` to use a 60-second HTTP timeout and retry safe read/query requests up to three attempts with 2-second and 5-second backoff.
- Booking/cancellation mutation POSTs and login POSTs are deliberately not retried to avoid duplicate actions after an ambiguous response timeout.
- Increased OpenClaw cron job `bf111852-60da-438a-af4b-68ae9f760aa7` timeout from 180 to 300 seconds.
- Verified compilation, simulated recovery on the third attempt, a live dry-run, and the production wrapper. Both upcoming bookings were already synchronized; no calendar events were changed.
